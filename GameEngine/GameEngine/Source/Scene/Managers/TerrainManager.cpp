#include "TerrainManager.h"

void TerrainManager::ConnectTerrainToMesh(Entity* entity)
{
	/*
	if (entity->HasComponent<TerrainComponent> && entity->HasComponent<MeshComponent>)
	{
		if (auto mesh = dynamic_cast<Grid*>(entity->GetComponent<MeshComponent>()->Get_Renderable()))
		{
			//Get type of Terrain
			switch ()
			{
				case PERLIN_NOISE;
				case HEIGHT_MAP;
			}
		}
		else
		{
			//TerrainComponent error -> Not suitable meshcomponent
		}
	}
	*/
}